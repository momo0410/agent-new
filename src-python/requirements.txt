# FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=14.0

# SSH
asyncssh>=2.18.0
paramiko>=3.5.0

# Encryption
cryptography>=43.0.0

# Data
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Utilities
python-multipart>=0.0.26
# Starlette's TestClient still relies on the pre-0.28 httpx constructor.
httpx>=0.27.0,<0.28.0
tomli>=2.0.0

# P1: Skill semantic search (optional - falls back to TF-IDF if unavailable)
fastembed>=0.8.0
